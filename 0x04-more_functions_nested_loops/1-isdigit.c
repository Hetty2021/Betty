#include "main.h"
/**
 * _isdigit - if c is a digit
 *@c: print int
 * Return: Always 1 if digit
 * Else, return 0.
 */

int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}

else
{
return (0);
}
}
